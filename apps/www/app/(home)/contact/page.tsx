import Contact from "@/components/ui/contact";

export const metadata = {
  title: "Contact - RefRef",
  description: "Get in touch with the authors of RefRef for any questions or support.",
};

export default function ContactPage() {
  return <Contact />;
}